  #Filters in pandas
  filt=df["<col_name>"]== "searching string" --> it wil returns the matched values with true or flase only
  df.apply(filt) or df[filt] --> will give all columns of matched strings
  df.loc(filter,'col_to_display')
  #To filter the strings in data frame
  df['col_name'].str.contains('value') --> gives all values where string  matches
   we can perform and ,or operation by using & ,|
   filt=(df["<col_name>"]== "searching string") & (df["<col_name>"]== "searching string")
   df.loc[filt,'wanted_column']
   we can filter a string in list by usinf "isin" as follows --> countries=['ind','aus','eng']
   filt=df['count'].isin(countires)
   df.loc[filt,'column_name']
   
   
   
  
